'use strict';

const recordRouter = require('./record-router');
const loginAdminRouter = require('./login-admin-router');
const loginCompanyRouter = require('./login-company-router');
const searchRouter = require('./record-router');

module.exports = {
    recordRouter,
    loginAdminRouter,
    loginCompanyRouter,
    searchRouter,
};
